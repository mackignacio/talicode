# @talicode.ai/core

TaliCode **core** module — core engine (config, git, reporting, usage, provider seam).

> **Reserved placeholder.** This package name is reserved for a future modular
> release of TaliCode. To use TaliCode today, install the full CLI:
>
> ```bash
> npm install -g talicode
> ```
>
> See the [TaliCode repository](https://github.com/mackignacio/talicode) for details.
