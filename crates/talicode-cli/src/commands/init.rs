// SPDX-License-Identifier: MIT
//! `tali init` — scaffold a `config.tali` + `skills/` in the current repo.
//!
//! Implements #5 (command surface). The real scaffold (write config, create
//! `skills/`, gitignore `.talicode/`) lands in #7.

pub fn run() -> anyhow::Result<()> {
    println!("tali init: not yet implemented (see #7)");
    Ok(())
}
