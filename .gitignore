# Rust build output
/target

# TaliCode local state (token-usage ledger, etc.)
.talicode/

# npm distribution layer
node_modules/
npm/**/tali
npm/**/tali.exe

# Secrets — never commit tokens (e.g. NPM_TOKEN)
.env
.env.*

# OS / editor
.DS_Store
